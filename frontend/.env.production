# Vercel + Railway split deployment
# Set this to your Railway backend URL in the Vercel dashboard under
# Project Settings → Environment Variables
# Leave blank for local / Docker use (frontend and backend run on same server)
VITE_API_URL=
